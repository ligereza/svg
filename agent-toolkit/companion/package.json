{
  "name": "context-shelf-companion",
  "version": "0.1.0",
  "private": true,
  "main": "main.cjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.cjs && node --check preload.cjs && node --check renderer.js"
  },
  "devDependencies": {
    "electron": "^42.0.0"
  }
}
